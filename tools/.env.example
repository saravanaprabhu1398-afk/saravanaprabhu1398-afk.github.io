# Copy to tools/.env and fill in. tools/.env is gitignored — never commit it.
# Values come from https://www.linkedin.com/developers/apps -> your app -> Auth tab.

LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_REDIRECT_URI=http://localhost:8000/callback
